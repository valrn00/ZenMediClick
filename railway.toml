[build]
builder = "NIXPACKS"

[setup]
# Obligamos a Railway a usar Python en lugar de Node
nixPkgs = ["python311", "pip"]

[deploy]
# Comando de ejecución del backend
start = "uvicorn backend.app.main:app --host 0.0.0.0 --port 8000"
